In class we worked on fromscratch.cpp

open returns a positive num if file could be opened, the numvber returned is called a file descriptor

last part of the assignment should open a file and place on screen, 1st should read input from the keyboard

read from standard input, means read fom 0, standard input is 0

bonus mark to find how to return a strruct here:         printf("The file's owner is: %s\n", getpwuid(mybuf.st_uid)->pw_name);
, may need to include more "include statements"