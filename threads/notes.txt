valgrind --leak-check=yes ./demo

Note: By next lab in 2 weeks, Need to learn how to make these p_thread functions return an integer

We will create threads, we don't want to exit cause that will stop the entire program
we will use pthread_exit to exit threads, as t's a more powerful eturn system
How much memomry is shared between threadS? Important to know
P thread exit makes sure you ony exit that thread, we need to make sure if we have ashared resource,
  something can finish a job before going on to the next job, we will us mutex for this

pthead_...
    create starts thread on a speciic function, it's like exec
    self to get thread id
    exit to escape a thread
    join equivalent of wait to get the id
    detach if you dn't want to wait

in the console
g++ sample.cpp -pthread, -pthread gives the thread-safe function, either this or you could define _REENTRANT and link lpthread

POSIX is a standard for unix functions, posix functions return 0 for success, != 0 for fail

Console:

man pthreads shows a list of the pthread library functions

man strtok is more safe
pthread create has 4 arduments, thread id, attr t should be a null pointer since we barely use it, 
                        function signature returns a void pointer and only 1 pointer paremeter 

A mutex is a special type of account that doesn't go below 0 and is 1 by default

pthread mutex_trylock tires if another process can be run (bahroom analogry)